build:
    tests:
        override:
            -
                command: 'php tools/phpunit.phar test --coverage-clover=coverage'
                coverage:
                    file: 'coverage'
                    format: 'php-clover'