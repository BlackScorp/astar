build:
    tests:
        override:
            -
                command: 'php tools/phpunit.phar test --coverage-clover=coverage.xml --whitelist=src'
                coverage:
                    file: 'coverage.xml'
                    format: 'php-clover'